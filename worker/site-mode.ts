// このファイルは frelio-cli が config.json の siteMode から自動生成する（手動編集しない / Issue #184）。
// siteMode を変えるには `npx @c-time/frelio-cli set-mode <mode>` 実行後にコンテンツを再デプロイする。
// 値: live | prelaunch | maintenance | closed
export const SITE_MODE = 'live'
